FROM python:3.11-slim

# Create unprivileged user for sandbox and make workspace accessible to nobody
RUN groupadd -r judge && \
    useradd -r -g judge -m -s /bin/bash judge && \
    mkdir -p /workspace && \
    chown -R nobody:nogroup /workspace && \
    chmod 777 /workspace

USER judge
WORKDIR /workspace
